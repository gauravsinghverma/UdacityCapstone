#Capstone Config
version: 2.1


jobs:
  lintapp:
    executor: python
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: |
            make install
      - run:
          name: run lint
          command: |
            make lint

workflows:
  deployment:
    jobs:
      - lintapp